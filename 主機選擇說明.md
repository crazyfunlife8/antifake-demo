# 主機選擇說明：共享主機 vs VPS

## 一、什麼是「主機」？

網站要讓外部用戶連上，必須放在一台「永遠開著的電腦」上運行，這台電腦就是「主機」（伺服器）。  
目前市場上最常見的兩種租用方案是**共享主機**與 **VPS**。

---

## 二、共享主機（Shared Hosting）

### 概念
類似「辦公大樓的格子辦公室」——許多用戶共用同一台實體機器，彼此隔離但受統一規定限制。

### 特性
- 管理介面簡單，上傳檔案即可運作
- **只支援特定技術**，台灣大多數共享主機僅支援 **PHP + MySQL**
- 不允許用戶自行安裝軟體或啟動自訂程序
- 適合：WordPress、靜態網站、傳統 PHP 網站

---

## 三、VPS（Virtual Private Server，虛擬私人伺服器）

### 概念
類似「獨立套房」——雖然實體機器與他人共用，但你擁有一個完全隔離的獨立環境，可以自由安裝軟體、執行任何程序。

### 特性
- 需要透過終端機（SSH）遠端操作，學習曲線較高
- **支援任何程式語言**（Python、Node.js、Ruby 等）
- 可自行安裝套件、資料庫、設定環境
- 適合：需要特定執行環境的應用程式、Django / Laravel / Node.js 等框架

---

## 四、兩者比較

| 項目 | 共享主機 | VPS |
|------|----------|-----|
| 月費（參考） | NT$100–200 | NT$150–400 |
| 支援語言 | 通常僅 PHP | 任意（Python、PHP 等） |
| 自行安裝套件 | 不行 | 可以 |
| 管理難度 | 低 | 中（需基本 Linux 指令） |
| 彈性 | 低 | 高 |
| 適合本專案 | **不支援** | **支援** |

---

## 五、為何本專案必須使用 VPS？

本專案後端使用 **Django（Python 框架）** 開發，具有以下技術特性：

1. **需要常駐執行 Python 程序**  
   Django 網站必須有一個持續運行的程序（透過 Gunicorn）來接收用戶請求。共享主機不允許用戶啟動自訂程序，因此無法運作。

2. **需要安裝 Python 套件**  
   專案依賴多個 Python 套件（如 qrcode、openpyxl、Pillow 等），必須透過 `pip install` 安裝。共享主機不提供此能力。

3. **需要執行資料庫初始化指令**  
   Django 部署時必須執行 `migrate` 等管理指令，共享主機無法提供終端機執行環境。

4. **資料庫為 SQLite 檔案**  
   本專案使用 SQLite，資料以檔案形式存放於伺服器磁碟，VPS 可直接支援，無需額外設定雲端資料庫。

---

## 六、若改用共享主機需要的工程

若堅持使用共享主機（PHP + MySQL），整個後端必須**從零以 PHP 重寫**，包含：

- 防偽碼查詢驗證邏輯
- QR Code 產生功能
- 管理後台（Django Admin 需完全重刻）
- 批量匯入、匯出 Excel
- 異常偵測報表
- Session 管理、6 語系切換

估計工程量為現有開發量的 **2–3 倍**，且需從頭測試。

---

## 七、建議

| 方案 | 建議 |
|------|------|
| 直接採購 VPS | ✅ 現有專案可直接部署，費用差異不大 |
| 改用共享主機 | ❌ 需大幅重寫，成本遠超主機費用節省的金額 |

**結論：選用 VPS 是最符合成本效益的方案。**  
現有專案架構完整，部署至 VPS 只需環境設定，無需任何程式碼改寫。

---

*本文件由開發端提供，供業主評估主機採購方案參考。*
